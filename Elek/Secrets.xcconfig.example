// Copy to Secrets.xcconfig and fill in the token that matches the Worker's
// AUTH_TOKEN secret (see worker/README.md). Leave empty to talk to a resolver
// that has no AUTH_TOKEN set.
ELEK_DOH_TOKEN =
